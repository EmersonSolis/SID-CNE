#!/bin/bash

function createOrg1() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/cne.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/cne.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:7054 --caname ca-cne --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-cne.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-cne.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-cne.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-cne.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/cne.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy org1's CA cert to org1's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/cne.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/cne/ca-cert.pem" "${PWD}/organizations/peerOrganizations/cne.com/msp/tlscacerts/ca.crt"

  # Copy org1's CA cert to org1's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/cne.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/cne/ca-cert.pem" "${PWD}/organizations/peerOrganizations/cne.com/tlsca/tlsca.cne.com-cert.pem"

  # Copy org1's CA cert to org1's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/cne.com/ca"
  cp "${PWD}/organizations/fabric-ca/cne/ca-cert.pem" "${PWD}/organizations/peerOrganizations/cne.com/ca/ca.cne.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-cne --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-cne --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-cne --id.name cneadmin --id.secret cneadminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:7054 --caname ca-cne -M "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/msp" --csr.hosts peer0.cne.com --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/cne.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:7054 --caname ca-cne -M "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls" --enrollment.profile tls --csr.hosts peer0.cne.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/cne.com/peers/peer0.cne.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:7054 --caname ca-cne -M "${PWD}/organizations/peerOrganizations/cne.com/users/User1@cne.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/cne.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/cne.com/users/User1@cne.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://cneadmin:cneadminpw@localhost:7054 --caname ca-cne -M "${PWD}/organizations/peerOrganizations/cne.com/users/Admin@cne.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/cne/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/cne.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/cne.com/users/Admin@cne.com/msp/config.yaml"
}

function createOrg2() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/oea.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/oea.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:8054 --caname ca-oea --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-oea.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-oea.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-oea.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-oea.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/oea.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy org2's CA cert to org2's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/oea.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/oea/ca-cert.pem" "${PWD}/organizations/peerOrganizations/oea.com/msp/tlscacerts/ca.crt"

  # Copy org2's CA cert to org2's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/oea.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/oea/ca-cert.pem" "${PWD}/organizations/peerOrganizations/oea.com/tlsca/tlsca.oea.com-cert.pem"

  # Copy org2's CA cert to org2's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/oea.com/ca"
  cp "${PWD}/organizations/fabric-ca/oea/ca-cert.pem" "${PWD}/organizations/peerOrganizations/oea.com/ca/ca.oea.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-oea --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-oea --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-oea --id.name oeaadmin --id.secret oeaadminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:8054 --caname ca-oea -M "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/msp" --csr.hosts peer0.oea.com --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/oea.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:8054 --caname ca-oea -M "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls" --enrollment.profile tls --csr.hosts peer0.oea.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/oea.com/peers/peer0.oea.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:8054 --caname ca-oea -M "${PWD}/organizations/peerOrganizations/oea.com/users/User1@oea.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/oea.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/oea.com/users/User1@oea.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://oeaadmin:oeaadminpw@localhost:8054 --caname ca-oea -M "${PWD}/organizations/peerOrganizations/oea.com/users/Admin@oea.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/oea/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/oea.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/oea.com/users/Admin@oea.com/msp/config.yaml"
}

function createOrderer() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/ordererOrganizations/example.com

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/ordererOrganizations/example.com

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:9054 --caname ca-orderer-cne --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer-cne.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer-cne.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer-cne.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer-cne.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy orderer org's CA cert to orderer org's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/msp/tlscacerts/tlsca.example.com-cert.pem"

  # Copy orderer org's CA cert to orderer org's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/tlsca/tlsca.example.com-cert.pem"

  infoln "Registering orderer"
  set -x
  fabric-ca-client register --caname ca-orderer-cne --id.name orderer --id.secret ordererpw --id.type orderer --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the orderer admin"
  set -x
  fabric-ca-client register --caname ca-orderer-cne --id.name ordererAdmin --id.secret ordererAdminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the orderer msp"
  set -x
  fabric-ca-client enroll -u https://orderer:ordererpw@localhost:9054 --caname ca-orderer-cne -M "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp" --csr.hosts orderer.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml" "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp/config.yaml"

  infoln "Generating the orderer-tls certificates"
  set -x
  fabric-ca-client enroll -u https://orderer:ordererpw@localhost:9054 --caname ca-orderer-cne -M "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls" --enrollment.profile tls --csr.hosts orderer.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the orderer's tls directory that are referenced by orderer startup config
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/tlscacerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/ca.crt"
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/signcerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/server.crt"
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/keystore/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/server.key"

  # Copy orderer org's CA cert to orderer's /msp/tlscacerts directory (for use in the orderer MSP definition)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp/tlscacerts"
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/tlscacerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp/tlscacerts/tlsca.example.com-cert.pem"

  infoln "Generating the admin msp"
  set -x
  fabric-ca-client enroll -u https://ordererAdmin:ordererAdminpw@localhost:9054 --caname ca-orderer-cne -M "${PWD}/organizations/ordererOrganizations/example.com/users/Admin@example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml" "${PWD}/organizations/ordererOrganizations/example.com/users/Admin@example.com/msp/config.yaml"
}

